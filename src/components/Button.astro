---
interface Props {
  variant?: "primary" | "secondary" | "accent" | "outline";
  size?: "sm" | "md" | "lg";
  href?: string;
  type?: "button" | "submit" | "reset";
  class?: string;
  disabled?: boolean;
  icon?: boolean;
}

const {
  variant = "accent",
  size = "md",
  href,
  type = "button",
  class: className,
  disabled = false,
  icon = false,
} = Astro.props;

const baseStyles = "inline-block font-bold uppercase tracking-wider transition-all duration-300";

const variants = {
  primary: "bg-black text-white hover:bg-[#F4C430] hover:text-black",
  secondary: "bg-white text-black hover:bg-[#F4C430] hover:text-black border-2 border-black",
  accent: "bg-[#F4C430] text-black hover:bg-[#e5b52a]",
  outline: "border-2 border-[#F4C430] text-[#F4C430] bg-transparent hover:bg-[#F4C430] hover:text-black",
};

const sizes = {
  sm: "px-6 py-2 text-sm",
  md: "px-12 py-4 text-lg",
  lg: "px-16 py-5 text-xl",
};

const disabledStyles = disabled ? "opacity-50 cursor-not-allowed" : "cursor-pointer";

const shadowStyle = variant === "accent" ? "box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.8);" : "";

const buttonClass = `${baseStyles} ${variants[variant]} ${sizes[size]} ${disabledStyles} ${className || ""}`;

const Tag = href ? "a" : "button";
---

<Tag
  href={href}
  class={buttonClass}
  style={shadowStyle}
  {...href ? {} : { type }}
  {...disabled ? { disabled: true } : {}}
>
  <span class="inline-flex items-center gap-3">
    <slot />
    {icon && (
      <svg
        class="w-5 h-5 transition-transform duration-300 group-hover:translate-x-1"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        stroke-width="3"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          d="M13 7l5 5m0 0l-5 5m5-5H6"
        />
      </svg>
    )}
  </span>
</Tag>

<style>
  a,
  button {
    border-radius: 0 !important;
  }

  button:disabled {
    pointer-events: none;
  }

  a:hover svg,
  button:hover:not(:disabled) svg {
    transform: translateX(4px);
  }
</style>
