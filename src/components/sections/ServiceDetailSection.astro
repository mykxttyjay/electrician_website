---
import ServiceCard from '../ui/ServiceCard.astro';

interface Props {
  image: string;
  imageAlt: string;
  title: string;
  description: string;
  services: Array<{ name: string; link?: string }>;
  serviceCards?: Array<{ name: string; image: string; link?: string }>;
}

const { image, imageAlt, title, description, services, serviceCards } = Astro.props;
---

<section class="bg-[#F8F8F8] py-20 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto">
    <div class="grid grid-cols-1 lg:grid-cols-[1.99fr_0.8fr] gap-6 items-start">
      <!-- Left: Image and Description -->
      <div>
        <img 
          src={image} 
          alt={imageAlt}
          class="w-full h-[383px] object-cover mb-6 rounded-lg"
        />
        <h2 class="text-4xl font-bold text-black mb-4">{title}</h2>
        <p class="text-gray-700 leading-relaxed mb-8" style="font-size: 22px;">
          {description}
        </p>

        <!-- Service Cards Grid -->
        {serviceCards && serviceCards.length > 0 && (
          <div class="grid grid-cols-4 gap-6">
            {serviceCards.map((card) => (
              <ServiceCard name={card.name} image={card.image} />
            ))}
          </div>
        )}
      </div>

      <!-- Right: Services List -->
      <div class="flex flex-col gap-6 justify-end items-end">
        <!-- Our Services Card -->
        <div class="bg-[#1a1a1a] p-8 inline-block rounded-lg">
          <h3 class="text-2xl font-bold text-white mb-6">Our Services</h3>
          <div class="space-y-4 flex flex-col items-end">
          {services.map((service, index) => (
            <a 
              href={service.link || '#'}
              class={`flex items-center justify-between px-6 py-4 transition-all duration-300 group min-w-[270px] ${
                index === 0 
                  ? 'bg-[#F4C430] text-black hover:bg-[#e5b52a]' 
                  : 'bg-[#2a2a2a] text-white hover:bg-[#F4C430] hover:text-black'
              }`}
            >
              <span class="font-bold uppercase tracking-wide" style="font-size: 13px;">{service.name}</span>
              <svg class="w-4 h-4 transition-transform group-hover:translate-x-1 flex-shrink-0 ml-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          ))}
        </div>
        </div>

        <!-- Get Started Today Card -->
        <div class="bg-[#1a1a1a] p-8 inline-block rounded-lg">
          <h3 class="text-2xl font-bold text-white mb-4 min-w-[270px]">Get Started Today</h3>
          <p class="text-gray-300 mb-6 leading-relaxed min-w-[270px]">
            Our certified electricians are ready to assist you with electrical solutions.
          </p>

          <!-- Contact Info -->
          <div class="space-y-4 mb-6 min-w-[270px]">
              <div class="flex items-center gap-3">
                <svg class="w-6 h-6 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                </svg>
                <span class="text-gray-300 font-medium">+(234) 567 8912</span>
              </div>

              <div class="flex items-center gap-3">
                <svg class="w-6 h-6 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                </svg>
                <span class="text-gray-300 font-medium">info@electrixa.com</span>
              </div>

              <div class="flex items-start gap-3">
                <svg class="w-6 h-6 text-gray-300 flex-shrink-0 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                </svg>
                <span class="text-gray-300 font-medium">Los Angeles, California</span>
              </div>
            </div>

            <!-- Get In Touch Button -->
            <a 
              href="/contact" 
              class="block w-full bg-[#F4C430] text-black font-bold text-center py-4 rounded-lg hover:bg-[#e5b52a] transition-all duration-300 uppercase tracking-wide min-w-[270px]"
            >
              Get In Touch
            </a>
        </div>
      </div>
    </div>
  </div>
</section>
